# 07-IPC/Message Flow Map

## Overview
- All IPC channels, message types, and handlers (main ↔ preload ↔ renderer).
- Table or diagram for quick reference.

## TODO: Extract from preload.ts, main.ts, and contracts.
