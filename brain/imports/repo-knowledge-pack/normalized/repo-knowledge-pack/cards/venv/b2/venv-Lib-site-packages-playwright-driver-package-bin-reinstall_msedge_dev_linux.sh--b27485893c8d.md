---
schema_version: 1
pack_id: "repo-knowledge-pack"
card_type: "file-summary"
source_path: ".venv/Lib/site-packages/playwright/driver/package/bin/reinstall_msedge_dev_linux.sh"
source_name: "reinstall_msedge_dev_linux.sh"
top_level: ".venv"
surface: "other"
classification: "neutral"
kind: "shell"
language: "shell"
extension: ".sh"
score: 4
selected_rank: 4488
content_hash: "fa8327d071bfa201e518cd4a895da4afb4b192aa1b5eb34e0cf64f297548ddac"
generated_at: "2026-04-02T15:23:30.410Z"
tags:
  - "neutral"
  - "other"
  - "sh"
  - "shell"
headings:
  - "1. make sure to remove old dev if any."
  - "2. Install curl to download Microsoft gpg key"
  - "3. Add the GPG key, the apt repo, update the apt cache, and install the package"
  - "GnuPG is not preinstalled in slim images"
---

# .venv/Lib/site-packages/playwright/driver/package/bin/reinstall_msedge_dev_linux.sh

> Text asset; headings 1. make sure to remove old dev if any. / 2. Install curl to download Microsoft gpg key / 3. Add the GPG key, the apt repo, update the apt cache, and install the package

## Key Signals

- Source path: .venv/Lib/site-packages/playwright/driver/package/bin/reinstall_msedge_dev_linux.sh
- Surface: other
- Classification: neutral
- Kind: shell
- Language: shell
- Top level: .venv
- Score: 4
- Tags: neutral, other, sh, shell
- Headings: 1. make sure to remove old dev if any. | 2. Install curl to download Microsoft gpg key | 3. Add the GPG key, the apt repo, update the apt cache, and install the package | GnuPG is not preinstalled in slim images

## Lookup Notes

- Use the card path in lookup.json to jump directly to this summary.
- Open the source from this card with the repository-relative path.
- Primary lookup terms: .venv, neutral, other, sh, shell
- Source link target: .venv/Lib/site-packages/playwright/driver/package/bin/reinstall_msedge_dev_linux.sh

## Excerpt

~~~shell
#!/usr/bin/env bash

set -e
set -x

if [[ $(arch) == "aarch64" ]]; then
  echo "ERROR: not supported on Linux Arm64"
  exit 1
fi

if [ -z "$PLAYWRIGHT_HOST_PLATFORM_OVERRIDE" ]; then
  if [[ ! -f "/etc/os-release" ]]; then
    echo "ERROR: cannot install on unknown linux distribution (/etc/os-release is missing)"
    exit 1
  fi

  ID=$(bash -c 'source /etc/os-release && echo $ID')
  if [[ "${ID}" != "ubuntu" && "${ID}" != "debian" ]]; then
    echo "ERROR: cannot install on $ID distribution - only Ubuntu and Debian are supported"
    exit 1
  fi
fi

# 1. make sure to remove old dev if any.
if dpkg --get-selections | grep -q "^microsoft-edge-dev[[:space:]]*install$" >/dev/null; then
  apt-get remove -y microsoft-edge-dev
fi
~~~