---
schema_version: 1
pack_id: "repo-knowledge-pack"
card_type: "file-summary"
source_path: ".venv/Lib/site-packages/playwright/driver/package/lib/server/electron/loader.js"
source_name: "loader.js"
top_level: ".venv"
surface: "other"
classification: "neutral"
kind: "code"
language: "javascript"
extension: ".js"
score: 16
selected_rank: 4190
content_hash: "cb76eb08c38b00b92f8f40959031adca74cd2479719febfba5d8868650b3370f"
generated_at: "2026-04-02T15:23:30.410Z"
tags:
  - "code"
  - "js"
  - "neutral"
  - "other"
  - "scripts"
imports:
  - "../chromium/chromiumSwitches"
  - "electron"
---

# .venv/Lib/site-packages/playwright/driver/package/lib/server/electron/loader.js

> Code module; imports ../chromium/chromiumSwitches, electron

## Key Signals

- Source path: .venv/Lib/site-packages/playwright/driver/package/lib/server/electron/loader.js
- Surface: other
- Classification: neutral
- Kind: code
- Language: javascript
- Top level: .venv
- Score: 16
- Tags: code, js, neutral, other, scripts
- Imports: ../chromium/chromiumSwitches, electron

## Lookup Notes

- Use the card path in lookup.json to jump directly to this summary.
- Open the source from this card with the repository-relative path.
- Primary lookup terms: .venv, code, js, neutral, other, scripts
- Source link target: .venv/Lib/site-packages/playwright/driver/package/lib/server/electron/loader.js

## Excerpt

~~~javascript
"use strict";
const { app } = require("electron");
const { chromiumSwitches } = require("../chromium/chromiumSwitches");
process.argv.splice(1, process.argv.indexOf("--remote-debugging-port=0"));
for (const arg of chromiumSwitches()) {
  const match = arg.match(/--([^=]*)=?(.*)/);
  app.commandLine.appendSwitch(match[1], match[2]);
}
const originalWhenReady = app.whenReady();
const originalEmit = app.emit.bind(app);
let readyEventArgs;
app.emit = (event, ...args) => {
  if (event === "ready") {
    readyEventArgs = args;
    return app.listenerCount("ready") > 0;
  }
  return originalEmit(event, ...args);
};
let isReady = false;
let whenReadyCallback;
const whenReadyPromise = new Promise((f) => whenReadyCallback = f);
app.isReady = () => isReady;
app.whenReady = () => whenReadyPromise;
globalThis.__playwright_run = async () => {
  const event = await originalWhenReady;
  isReady = true;
  whenReadyCallback(event);
  originalEmit("ready", ...readyEventArgs);
~~~