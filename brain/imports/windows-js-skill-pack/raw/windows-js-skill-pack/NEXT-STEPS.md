# Next layer to add

To go beyond opening windows and actually manipulate controls inside arbitrary Windows apps, add one of these:

- Windows UI Automation bridge
- AutoHotkey v2 bridge
- PowerShell-backed admin/task bridge
- App-specific adapters for browsers, terminals, IDEs, launchers, vendor panels

Keep those behind approval rules. Do not let the model write raw shell commands from chat.
